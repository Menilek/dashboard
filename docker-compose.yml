version: "3.8"

services:
  backend:
    build: "./backend"
    ports:
      - "5000:5000"
    tty: true
    links:
      - frontend
  frontend:
    build: "./frontend"
    ports:
      - "3000:80"
#  docker-compose up --build
#  docker-compose down -v
